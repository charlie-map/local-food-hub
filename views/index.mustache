<!-- This is a static file -->
<!-- served from your routes in server.js -->
<!DOCTYPE html>
<html lang="en">
<head>
	<!-- import the webpage's stylesheet -->
	<link rel="stylesheet" href="/style.css" />

	<!-- import the webpage's client-side javascript file -->
	<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
</head>
<body>
	<header>
		<h1>Homepage for {{farm_name}}</h1>
	</header>
	<main>

		<div id = 'allStati'>{{#type}}{{#.}}
			<ul id="list">
				<span id = "title">{{titleoftype}}</span>
				<div id = 'statusoftype'>{{#row}}
					<span id="file_name"> {{FILE_NAME}}</span>
					<!-- <button class="deleteBoyAdmin" id="{{id}}" target="{{adminDecide}}"> Delete boy </button> -->
					{{#STATUS}}
						<!-- <a id="fileID" href="/{{FILE_URL}}">Click here to fill out</a> -->
						<form>
							<button id="fill_out" formaction="{{FILE_URL}}">Fill out</button>
						</form>
						<form>
							<button class="ignore_file" id="{{USERNAME}}/{{FILE_ID}}">Ignore</button>
						</form>
					{{/STATUS}}
					{{^STATUS}}
						<form>
							<button formaction="{{FILE_URL}}">Completed</button>
						</form>
					{{/STATUS}}
				{{/row}}</div>
			</ul>
		{{/.}}{{/type}}</div>
	</main>
	<script src="/script.js"></script>
</body>
</html>